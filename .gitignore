# Secrets — this is a PUBLIC repo. Never commit these.
.env
.env.*
!.env.example
token.json
credentials.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Scratch — regenerable, per WAT convention
.tmp/

# OS
.DS_Store
Thumbs.db

# NOTE: data/ is deliberately NOT ignored. The captured snapshots are the
# point of this repo — git history is the drift record.
